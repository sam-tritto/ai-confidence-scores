# Workspace Customization Rules

## Privacy & Security Rules
- **NEVER** view, read, search, or output the contents of `.env` files or secret files.
- Always refer to template files like `.env.example` for variable names and configuration options.
